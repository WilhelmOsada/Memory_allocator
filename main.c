#include "heap.h"
int main() {
    //This program uses the function custom_sbrk(), which is not 
    //a function written by me. Therefore i cannot provide an
    //example of how the code works in practice.
    return 0;
}

